import { Shield, FileText, Scale, Clock, Send, Calendar, CheckCircle } from 'lucide-react';

// Shared icon-name -> lucide component map for roadmap/stage-stage UI
// (FlowRoadmap.jsx and SimpleFlowRunner.jsx's stage tracker both use this).
export const STAGE_ICONS = { Shield, FileText, Scale, Clock, Send, Calendar, CheckCircle };

export default STAGE_ICONS;
