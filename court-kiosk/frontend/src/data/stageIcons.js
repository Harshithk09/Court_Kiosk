import { Shield, FileText, Scale, Clock, Send, Calendar, CheckCircle, ClipboardList } from 'lucide-react';

// Shared icon-name -> lucide component map for roadmap/stage UI
export const STAGE_ICONS = { Shield, FileText, Scale, Clock, Send, Calendar, CheckCircle, ClipboardList };

export default STAGE_ICONS;
